### App Objects

This directory bundles example applications that wire the shared SQL runner into a specific dataset.  
Each module defines its tables, prompt customisations, explicit config objects, and exposes a ready-to-use `SqlLLM`.  
